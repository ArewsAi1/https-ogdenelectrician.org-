name: Preference Taxidermy Asset Upload Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary upload workflow disabled after migration testing."
